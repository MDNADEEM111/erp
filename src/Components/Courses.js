import React from 'react';

const Courses = () => {
    return (
        <div>
            <h2>Available Courses</h2>
            {/* Add your courses listing here */}
        </div>
    );
};

export default Courses;
